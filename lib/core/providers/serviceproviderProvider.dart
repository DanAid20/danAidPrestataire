import 'package:flutter/material.dart';

class ServiceProvider with ChangeNotifier {
  String _category, _orderRegistrationCertificate, _cniName, _contactEmail, _id, _contactName;
  DateTime _createdDate;
}